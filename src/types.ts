export interface iHeroBanner {
    heading: {
        text: string;
        className?: string
    }[];
    description: string;
    image?: string; // Optional property
}

// Define the type for the array

